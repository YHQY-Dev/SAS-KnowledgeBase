# SAS Knowledge Base

静态小角散射（SAXS / SANS / GISAXS）中文知识站。

## 本地预览

用任意静态服务器打开本仓库根目录，例如：

```bash
npx --yes serve .
```

或直接用浏览器打开 `index.html`（`file://` 可用）。

## 内容

- `learn/` 学习路径
- `topics/` 专题手册
- `glossary/` 术语表
- `catalog/` 文献目录

站内搜索：顶栏「搜索」或 Ctrl+K。
